module.exports = {
  availableButtons: [
    // these appear as buttons
    { code: "DECSPECIAL", discount: 20 },
  ],

  // these DO NOT appear as buttons but can be manually entered
  hiddenCodes: [{ code: "DECSPECIAL", discount: 20 }],
};
